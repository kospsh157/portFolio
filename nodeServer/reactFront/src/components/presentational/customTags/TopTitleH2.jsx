import styled from 'styled-components';

const TopTitleH2 = styled.h2`
    color: rgba(0, 0, 0, 0.4);
    position: sticky;
    top: 0;
    &:hover {
        color: rgba(0, 0, 0, 1);
    };
`;

export default TopTitleH2;